<h1>Add a movie to <%= @list.name %></h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>
  <%= f.button :submit, "Add to list", class: "btn btn-flat" %>
<% end %>

<br><%= link_to "Back to lists", lists_path, class: "btn btn-flat" %>
