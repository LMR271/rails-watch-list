<h1>New list</h1>

<%= simple_form_for @list do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file%>
  <%= f.button :submit, "Create list", class: "btn btn-flat" %>
<% end %>

<br><%= link_to "Back to lists", lists_path, class: "btn btn-flat" %>
